version: '3.9'
services:
  english-explorer:
    build: .
    ports:
      - "5000:5000"
    environment:
      SECRET_KEY: change-me
      PARENT_PORTAL_PASSWORD: parents123
    volumes:
      - ./instance:/app/instance
