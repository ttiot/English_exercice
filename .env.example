SECRET_KEY=gsqiuyfqdhkjdhdciluhgcisfdoqiysf57567sfqf
DEFAULT_ADMIN_PASSWORD='admin123'
DATA_DIR=/data
# Correction: utiliser 3 slashes au lieu de 4 pour SQLite
DATABASE_URL=sqlite:///data/app.db

# Chiffrement Fernet de la clé OpenAI stockée en BDD.
# Génère-la avec : python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

# Repli OpenAI quand aucun OpenAIConfig n'est actif en BDD (admin > Configuration IA).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1
